# TODO

1. enforce consistent JSON schema for items, default null or empty array etc.
1. image fields should reference repos `images/` dir over url (handle updating urls in ts or something)
1. improve ids (esp ./data/quests/)
